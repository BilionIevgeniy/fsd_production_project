{
  "presets": [
    ["@babel/preset-env", { "targets": { "node": "current" } }], // modern JS in old, targets current Node since this config is used only by Jest
    "@babel/preset-typescript", // remove all ts that browser dont understand
    ["@babel/preset-react", { "runtime": "automatic" }] // understand JSX
  ]
}
